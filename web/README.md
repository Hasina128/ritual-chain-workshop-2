# The Desk (paper UI)

Newspaper-style board for RitualPredict. Cream stock, ink rules, YES/NO tickets.

```bash
pnpm install
cp .env.example .env.local
pnpm dev
```

The wire is `/api/oracle/eth`. A TEE cannot fetch localhost — tunnel it before live settle.
